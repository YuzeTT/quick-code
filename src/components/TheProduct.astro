---
export interface Props {
	title: string;
  remarks: string
  price: string
}

const { title, remarks, price } = Astro.props;
---

<div class="flex items-center justify-between mt-3">
  <div>
    <div class="text-xl font-bold">{ title }</div>
    <div>{ remarks }</div>
  </div>
  <div class="font-bold text-2xl text-red-500">￥{ typeof price == 'number'? price.toFixed(2) : price }</div>
</div>